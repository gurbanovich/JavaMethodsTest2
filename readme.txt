Task varB:

1. Determine whether some value of k belongs to the intervals (n, m], [n, m), (n, m), [n, m].
2. Print the numbers from 1 to k as a matrix N x N from left to right and from top to bottom.
3. Find the roots of the quadratic equation. The parameters of the equation transfer
with the command line.
4. Enter a number from 1 to 12. Output to the console the name of the month corresponding to the given number. Check that the numbers are correct.